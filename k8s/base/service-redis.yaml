apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: roomler
spec:
  ports:
  - port: 6379
    protocol: TCP
    targetPort: 6379
  selector:
    app: redis
  sessionAffinity: None
  type: ClusterIP
