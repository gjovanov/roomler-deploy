apiVersion: v1
kind: Service
metadata:
  name: roomler
  namespace: roomler
spec:
  externalTrafficPolicy: Cluster
  ports:
  - nodePort: 30030
    port: 3000
    protocol: TCP
    targetPort: 3000
  selector:
    app: roomler
  sessionAffinity: None
  type: NodePort
